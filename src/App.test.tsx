import React from 'react';
import { render } from '@testing-library/react';
import '@testing-library/jest-dom';

describe('Dummy test', () => {
  it('App 페이지가 정상적으로 렌더링된다.', () => {
    expect(true).toEqual(true);
  });
});
